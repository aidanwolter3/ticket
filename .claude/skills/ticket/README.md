# ticket skill

This is the canonical copy of the `/ticket` Claude Code skill. It lives in the repository so all contributors get it automatically when working in this directory.

## Global copy

`~/.claude/skills/ticket/SKILL.md` is a symlink pointing here:

```
~/.claude/skills/ticket/SKILL.md -> /path/to/repo/.claude/skills/ticket/SKILL.md
```

Edit only this copy. The symlink keeps the global and project copies in sync.
